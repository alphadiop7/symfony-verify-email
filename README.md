# symfony-verify-email
# After cloning the repository, run the following commands:
#  symfony console doctrine:database:create
# symfony console make:migration
# symfony console doctrine:migrations:migrate
